
  # Ainus_UI (Community)

  This is a code bundle for Ainus_UI (Community). The original project is available at https://www.figma.com/design/KHcfGODDQE02Xc71l6beTq/Ainus_UI--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  